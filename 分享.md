# SecMind 融合程序协作分享说明

本文档对应当前融合项目：my-competition-secmind-baseline-f606c48。

当前项目包含：

- SecMind FastAPI 后端、任务编排、题型识别、安全报告和 WebSocket 实时事件。
- React/Vite 前端工作台、态势总览、AI 实时流、黑板探索图和路由确认页。
- 代码审计模块（本地工作区只读分析）。
- 逆向模块适配器（通过 SECMIND_REVERSE_BASE_URL 连接独立逆向服务）。
- Cairn 渗透模块适配器（通过 SECMIND_CAIRN_BASE_URL 连接独立 Cairn 服务）。

逆向项目和 Cairn 项目没有物理复制到本 ZIP 中，协作者需要单独运行它们，或把地址改为团队已经部署的服务地址。

## 一、解压后目录

建议把 ZIP 解压到一个不含中文和空格的目录，例如：

~~~text
C:\secwork\secmind
~~~

根目录中应看到：

~~~text
src\secmind              # Python 后端
frontend\                # React + Vite 前端
tests\                   # 后端测试
frontend\tests\          # 前端测试
Dockerfile
docker-compose.yml
.env.example
pyproject.toml
uv.lock
分享.md
~~~

不要手工修改源码中的绝对路径。程序默认使用相对路径：./data/inputs、./data/uploads、./data/runs 和 ./data/evaluations。路径相对于启动后端时的当前目录。

## 二、Windows 本地运行（推荐开发方式）

### 1. 后端

安装 Python 3.11–3.13 和 uv，然后执行：

~~~powershell
cd C:\secwork\secmind
uv sync --all-extras
Copy-Item .env.example .env
~~~

开发模式建议使用 8001 端口：

~~~powershell
$env:SECMIND_API_PORT="8001"
uv run secmind-api
~~~

健康检查：http://127.0.0.1:8001/health

如果端口被占用，不要同时启动第二个后端。可以换端口，例如：

~~~powershell
$env:SECMIND_API_PORT="8011"
~~~

### 2. 前端

另开 PowerShell：

~~~powershell
cd C:\secwork\secmind\frontend
npm ci
$env:VITE_DEV_BACKEND_TARGET="http://127.0.0.1:8001"
npm run dev
~~~

打开：http://127.0.0.1:5173/dashboard 或 http://127.0.0.1:5173/workbench。

如果后端换成 8011，前端必须同步改为：

~~~powershell
$env:VITE_DEV_BACKEND_TARGET="http://127.0.0.1:8011"
~~~

frontend/vite.config.js 是开发代理的特殊位置；它默认代理到 8001。开发服务器已在 5173 运行时，不要重复执行 npm run dev。

## 三、API Key 填写方式

### 方式 A：.env 文件（推荐）

在项目根目录创建 .env，不要修改或覆盖 .env.example：

~~~dotenv
SECMIND_DEMO_MODE=false
SECMIND_QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1
SECMIND_QWEN_API_KEY=在这里填入真实APIKey
SECMIND_PLANNER_MODEL=qwen-plus
SECMIND_WORKER_MODEL=qwen-turbo
SECMIND_FALLBACK_MODEL=qwen-max
~~~

如果使用兼容 OpenAI API 的其他服务，只修改 SECMIND_QWEN_BASE_URL 和模型名。例如 DeepSeek 应填服务商实际支持的模型 ID，不能照搬 qwen-*。

填写后必须重启后端。API Key 不会写入前端代码，也不应提交到 Git 或发送给协作者。

### 方式 B：当前 PowerShell 会话

~~~powershell
$env:SECMIND_DEMO_MODE="false"
$env:SECMIND_QWEN_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
$env:SECMIND_QWEN_API_KEY="真实APIKey"
uv run secmind-api
~~~

关闭 PowerShell 后这些变量会失效。不要把包含真实 Key 的命令复制到聊天记录、截图或公开仓库。

### Demo 模式

没有 API Key 时保持：

~~~dotenv
SECMIND_DEMO_MODE=true
SECMIND_QWEN_API_KEY=
~~~

系统使用确定性降级逻辑，适合协作者先验证界面、上传、路由和报告链路。

### 页面配置

模型与用量页面可以查看模型配置。运行时配置可能写入 data/model-runtime.json，该文件属于本机运行数据，不能打包分享。团队统一部署时仍建议通过 Secret 或环境变量注入 Key。

## 四、路径和外部模块适配

### 项目内部路径

.env 中以下路径可以使用相对路径，也可以使用绝对路径：

~~~dotenv
SECMIND_INPUT_ROOT=./data/inputs
SECMIND_RUN_ROOT=./data/runs
SECMIND_UPLOAD_ROOT=./data/uploads
SECMIND_EVALUATION_ROOT=./data/evaluations
~~~

Windows 绝对路径示例：

~~~dotenv
SECMIND_INPUT_ROOT=C:/secwork/secmind-data/inputs
SECMIND_RUN_ROOT=C:/secwork/secmind-data/runs
SECMIND_UPLOAD_ROOT=C:/secwork/secmind-data/uploads
SECMIND_EVALUATION_ROOT=C:/secwork/secmind-data/evaluations
~~~

优先使用 /，避免反斜杠转义问题。目录不存在时后端会创建；运行账户必须具有读写权限。

### 逆向和 Cairn 服务

~~~dotenv
SECMIND_REVERSE_BASE_URL=http://127.0.0.1:8002
SECMIND_CAIRN_BASE_URL=http://127.0.0.1:8000
~~~

如果服务运行在其他机器，例如：

~~~dotenv
SECMIND_REVERSE_BASE_URL=http://192.168.1.20:8002
SECMIND_CAIRN_BASE_URL=http://192.168.1.21:8000
~~~

必须确认协作者机器可以访问这些地址，并且目标处于已授权测试环境。URL 只在用户确认路由并启动渗透任务后交给 Cairn。

### Test3.0 目录

只有需要单题评测时才配置以下变量，并确保私有 Gold 和评分器不与前端共享：

~~~dotenv
SECMIND_BENCHMARK_DATASET_ROOT=C:/path/to/public-input
SECMIND_BENCHMARK_PRIVATE_ROOT=C:/path/to/private-gold
SECMIND_BENCHMARK_SCORER_ROOT=C:/path/to/isolated-scorer
SECMIND_BENCHMARK_PYTHON_EXECUTABLE=C:/path/to/scorer-venv/Scripts/python.exe
~~~

不要把这些私有目录复制进分享包。

## 五、Docker Compose 启动

前提：安装并启动 Docker Desktop，确认 Docker 引擎正常运行。

在项目根目录执行：

~~~powershell
cd C:\secwork\secmind
Copy-Item .env.example .env
~~~

编辑 .env，至少修改：

~~~dotenv
SECMIND_DEMO_MODE=true
POSTGRES_PASSWORD=请替换为随机强密码
~~~

有模型 Key 时再设置：

~~~dotenv
SECMIND_DEMO_MODE=false
SECMIND_QWEN_API_KEY=真实APIKey
~~~

启动：

~~~powershell
docker compose up --build -d
docker compose ps
docker compose logs -f api
~~~

访问：

- 前端：http://127.0.0.1:5173
- API：http://127.0.0.1:8000/health

### Docker 的特殊端口和路径

- Docker API 容器固定监听 8000，不能用开发模式的 8001 说明替代它。
- Docker 前端通过 Nginx 监听容器内 8080，宿主机映射为 5173。
- frontend/nginx.conf 中的 proxy_pass http://api:8000 使用 Compose 服务名，不能改成 127.0.0.1；容器内的 127.0.0.1 指向前端容器自身。
- docker-compose.yml 中 /app/data/... 是容器路径，不是 Windows 路径。需要挂载外部目录时，应在 volumes 中增加明确的 Docker Desktop 共享目录映射。
- Compose 默认使用 PostgreSQL 和 Qdrant 命名卷，数据不会写入源代码目录。

停止服务但保留数据：

~~~powershell
docker compose down
~~~

停止并删除 Compose 数据卷（会删除数据库、运行记录和 Qdrant 数据，谨慎使用）：

~~~powershell
docker compose down -v
~~~

## 六、协作者首次验收清单

1. 能访问 /health，返回 status: ok。
2. 前端顶部显示“后端在线”。
3. 新建任务时可以上传文件、输入靶场网址或粘贴文本。
4. 点击“上传并识别”后出现类型、置信度、识别依据和执行边界。
5. 代码文件通常路由到代码审计，PE/ELF 等样本路由到逆向，带授权靶场 URL 或端口扫描关键词的任务路由到渗透。
6. 只有点击“确认路由并启动”后才会创建实际运行任务；渗透任务还需要 Cairn 服务在线并通过审批。

## 七、常见问题

### 端口 5173 或 8001 被占用

这通常表示已有服务正在运行。先访问已有地址；如果必须更换端口，后端改 SECMIND_API_PORT，前端开发代理改 VITE_DEV_BACKEND_TARGET，Docker 端口则改 docker-compose.yml 的宿主机映射。

### 页面显示断开连接

确认：

~~~powershell
Invoke-WebRequest http://127.0.0.1:8001/health
~~~

开发模式下检查 VITE_DEV_BACKEND_TARGET 是否与后端端口一致；Docker 模式下检查前端是否通过 api:8000 连接 API。

### 模型 Base URL 报错

必须填写完整的绝对 URL，例如：

~~~text
https://dashscope.aliyuncs.com/compatible-mode/v1
~~~

不要把 Markdown 链接写法、方括号、查询参数或页面链接粘贴到 SECMIND_QWEN_BASE_URL。

### 逆向或渗透模块不可用

先确认 SECMIND_REVERSE_BASE_URL、SECMIND_CAIRN_BASE_URL 指向的是服务 API 根地址，而不是前端页面地址；再检查防火墙、端口和服务日志。模块不可用时，SecMind 会保留任务和证据链，并在报告中标记降级原因。

## 八、分享安全要求

- 分享前确认 .env 不在压缩包中。
- 不分享 data/、数据库、上传文件、模型运行配置、私有题库和评分器目录。
- 不分享 .venv、frontend/node_modules 和 frontend/dist，协作者可按锁文件重新安装。
- API Key、数据库密码、Cairn 授权信息必须通过私密渠道单独交付。
- 当前 ZIP 是源码分发包，不是包含所有外部服务的一键离线安装包。

