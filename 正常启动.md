# 真实测评环境正常启动指南

本文用于已经完成首次环境配置后的日常启动。

当前采用以下运行方式：

```text
统一平台 API：C:\kaifa\tool\an，端口 8001
前端：C:\kaifa\tool\an\frontend，端口 5173
逆向服务：C:\kaifa\tool\secmind，端口 8002
Cairn Server：C:\kaifa\tool\Cairn-worker-full，端口 8000
Cairn Worker：Windows 本地 claude CLI
模型 API：DeepSeek
```

所有渗透操作仅限已经授权的本地或隔离测试环境。

## 一、启动前检查

打开 PowerShell，执行：

```powershell
python --version
uv --version
node --version
npm --version
claude --version
```

检查端口是否已被占用：

```powershell
$ports = 5173,8000,8001,8002

Get-NetTCPConnection -State Listen -ErrorAction SilentlyContinue |
    Where-Object { $ports -contains $_.LocalPort } |
    Sort-Object LocalPort |
    Format-Table LocalAddress,LocalPort,OwningProcess
```

如果已有同一套服务在运行，不要重复启动对应服务。

## 二、启动 Cairn Server

打开 PowerShell 窗口一：

```powershell
Set-Location C:\kaifa\tool\Cairn-worker-full

uv run --project cairn cairn serve `
  --host 127.0.0.1 `
  --port 8000 `
  --db-path C:\kaifa\tool\Cairn-worker-full\datas\cairn\cairn.db
```

保持该窗口运行，不要按 `Ctrl+C`。

另开 PowerShell 验证：

```powershell
Invoke-WebRequest http://127.0.0.1:8000/projects
```

返回 JSON 或 HTTP 200 才能继续启动 Dispatcher。

## 三、启动 Cairn Dispatcher（本地 Claude CLI）

本方案使用本机 Claude CLI，因此必须使用 `dispatch.local.yaml`，不能使用 Docker Dispatcher。

打开 PowerShell 窗口二：

```powershell
Set-Location C:\kaifa\tool\Cairn-worker-full

$env:ANTHROPIC_BASE_URL="https://api.deepseek.com/anthropic"
$env:ANTHROPIC_AUTH_TOKEN="<YOUR_DEEPSEEK_API_KEY>"
$env:ANTHROPIC_MODEL="<AVAILABLE_DEEPSEEK_ANTHROPIC_MODEL>"
```

检查变量是否设置成功，不要输出完整密钥：

```powershell
Write-Host $env:ANTHROPIC_BASE_URL
Write-Host $env:ANTHROPIC_MODEL
Write-Host ("Token configured: " + [bool]$env:ANTHROPIC_AUTH_TOKEN)
```

确认配置文件使用本地 Claude Worker：

```powershell
Select-String -Path .\dispatch.local.yaml -Pattern 'server:|execution:|type:|ANTHROPIC_'
```

应看到以下配置含义：

```yaml
server: "http://127.0.0.1:8000"
execution: "local"
type: "claudecode"
```

启动 Dispatcher：

```powershell
uv run --project cairn cairn dispatch `
  --config C:\kaifa\tool\Cairn-worker-full\dispatch.local.yaml
```

保持该窗口运行。

## 四、启动逆向服务

打开 PowerShell 窗口三：

```powershell
Set-Location C:\kaifa\tool\secmind
uv sync --frozen
```

如果逆向项目的 `.env` 已经配置好真实模型，直接设置端口并启动：

```powershell
$env:SECMIND_API_HOST="127.0.0.1"
$env:SECMIND_API_PORT="8002"

uv run secmind-api
```

如果需要在本次会话中强制使用真实 DeepSeek 模型，可改用：

```powershell
$env:SECMIND_API_HOST="127.0.0.1"
$env:SECMIND_API_PORT="8002"
$env:SECMIND_MODEL_MODE="live"
$env:SECMIND_ALLOW_LIVE_MODELS="true"
$env:SECMIND_DEMO_MODE="false"
$env:SECMIND_QWEN_BASE_URL="https://api.deepseek.com"
$env:SECMIND_TRUSTED_MODEL_BASE_URLS="https://api.deepseek.com"
$env:SECMIND_MODEL_BASE_URL_PROVIDER="deepseek"
$env:SECMIND_DEEPSEEK_API_KEY="<YOUR_DEEPSEEK_API_KEY>"
$env:SECMIND_PLANNER_MODEL="<AVAILABLE_DEEPSEEK_MODEL>"
$env:SECMIND_WORKER_MODEL="<AVAILABLE_DEEPSEEK_MODEL>"
$env:SECMIND_FALLBACK_MODEL="<AVAILABLE_DEEPSEEK_MODEL>"

uv run secmind-api
```

验证逆向服务：

```powershell
Invoke-WebRequest http://127.0.0.1:8002/health/live
```

## 五、启动统一平台后端

打开 PowerShell 窗口四：

```powershell
Set-Location C:\kaifa\tool\an
```

确认 `.env` 存在；不存在时只复制模板，不覆盖已有配置：

```powershell
if (!(Test-Path .env)) {
    Copy-Item .env.example .env
}
```

启动真实模型模式并连接两个外部模块：

```powershell
$env:SECURITY_AGENT_API_HOST="127.0.0.1"
$env:SECURITY_AGENT_API_PORT="8001"
$env:SECURITY_AGENT_DEMO_MODE="false"
$env:SECURITY_AGENT_REVERSE_BASE_URL="http://127.0.0.1:8002"
$env:SECURITY_AGENT_PENETRATION_BASE_URL="http://127.0.0.1:8000"

uv run security-agent-api
```

如果模型 Key 已保存在 `C:\kaifa\tool\an\.env`，不需要再次写入 PowerShell。
如果没有保存到 `.env`，在当前窗口补充：

```powershell
$env:SECURITY_AGENT_MODEL_BASE_URL="https://api.deepseek.com"
$env:SECURITY_AGENT_MODEL_API_KEY="<YOUR_DEEPSEEK_API_KEY>"
$env:SECURITY_AGENT_PLANNER_MODEL="<AVAILABLE_DEEPSEEK_MODEL>"
$env:SECURITY_AGENT_WORKER_MODEL="<AVAILABLE_DEEPSEEK_MODEL>"
$env:SECURITY_AGENT_FALLBACK_MODEL="<AVAILABLE_DEEPSEEK_MODEL>"
```

验证统一平台：

```powershell
Invoke-WebRequest http://127.0.0.1:8001/health
```

API 文档：

```text
http://127.0.0.1:8001/docs
```

## 六、启动前端

打开 PowerShell 窗口五：

```powershell
Set-Location C:\kaifa\tool\an\frontend
```

首次安装或依赖发生变化时执行：

```powershell
npm ci
```

每次启动前端执行：

```powershell
$env:VITE_DEV_BACKEND_TARGET="http://127.0.0.1:8001"
npm run dev
```

访问：

```text
http://127.0.0.1:5173/workbench
```

应急响应页面：

```text
http://127.0.0.1:5173/incident-response
```

## 七、启动完成后的统一验收

在任意 PowerShell 窗口执行：

```powershell
Invoke-WebRequest http://127.0.0.1:8000/projects
Invoke-WebRequest http://127.0.0.1:8001/health
Invoke-WebRequest http://127.0.0.1:8002/health/live
```

检查前端代理：

```powershell
Write-Host $env:VITE_DEV_BACKEND_TARGET
```

应为：

```text
http://127.0.0.1:8001
```

进入工作台后，建议按以下顺序进行一次真实链路测试：

1. 上传一个测试文件或输入授权靶场目标。
2. 点击“上传并识别”。
3. 确认题型和路由结果。
4. 点击“确认路由并启动”。
5. 渗透任务检查 Cairn Dispatcher 日志是否出现 Claude CLI 执行记录。
6. 查看实时事件、报告和任务结果。

## 八、Docker 说明

本指南的 Cairn Worker 使用 Windows 本地 Claude CLI，因此不要用以下命令代替本地 Dispatcher：

```powershell
docker compose up --build -d
```

该命令会启动容器化 Dispatcher，不能调用 Windows 主机上的 `claude` 命令。

如果只需要单独启动统一平台 Docker 版本，执行：

```powershell
Set-Location C:\kaifa\tool\an

if (!(Test-Path .env)) {
    Copy-Item .env.example .env
}

docker compose build
docker compose up -d
docker compose ps
```

统一平台 Docker 模式会占用宿主机 `8000`，此时必须将 Cairn 映射到 `8011:8000`，并把渗透地址改为：

```text
http://127.0.0.1:8011
```

不要同时让统一平台 Docker 和 Cairn Docker 使用宿主机 `8000`。

## 九、常见问题

### 1. `cairn-server` 名称冲突

```text
Conflict. The container name "/cairn-server" is already in use
```

本指南默认使用本地 Cairn Server，不需要 Docker 容器。如果确实要运行 Cairn Docker，先检查旧容器：

```powershell
docker ps -a --filter "name=^cairn-server$"
docker ps -a --filter "name=^cairn-dispatcher$"
```

确认是停止的旧容器后删除：

```powershell
docker rm cairn-server
docker rm cairn-dispatcher
```

### 2. 前端显示断开连接

```powershell
Invoke-WebRequest http://127.0.0.1:8001/health
```

确认前端窗口设置了：

```powershell
$env:VITE_DEV_BACKEND_TARGET="http://127.0.0.1:8001"
```

然后重启 `npm run dev`。

### 3. Cairn Dispatcher 无法连接 Server

```powershell
Invoke-WebRequest http://127.0.0.1:8000/projects
```

确认 `dispatch.local.yaml` 使用：

```yaml
server: "http://127.0.0.1:8000"
runtime:
  execution: "local"
```

### 4. Claude CLI 或 DeepSeek 鉴权失败

```powershell
claude --version
Write-Host $env:ANTHROPIC_BASE_URL
Write-Host $env:ANTHROPIC_MODEL
Write-Host ("Token configured: " + [bool]$env:ANTHROPIC_AUTH_TOKEN)
```

确认 `ANTHROPIC_BASE_URL` 为：

```text
https://api.deepseek.com/anthropic
```

### 5. API Key 安全

不要把真实密钥写入：

```text
dispatch.local.yaml
dispatch.yaml
.env.example
Dockerfile
Git
截图或聊天记录
```

建议使用当前 PowerShell 会话变量或本机 `.env`，并确保 `.env` 已被 `.gitignore` 排除。

## 十、停止服务

本地运行的五个 PowerShell 窗口分别按：

```text
Ctrl+C
```

如果启动过 Docker Compose：

```powershell
docker compose down
```

不要在未备份数据的情况下使用：

```powershell
docker compose down -v
```

该命令会删除 PostgreSQL、Qdrant 或其他 Compose 数据卷。
