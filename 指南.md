# 安全任务平台首次启动与运行指南

本文用于 Windows 测试环境，覆盖统一平台、前端、Cairn 黑板/渗透外接服务和逆向外接服务。
所有目标必须是已授权的本地或隔离靶场。

## 一、首次启动前准备

安装以下工具并确认命令可用：

| 工具 | 建议版本 | 检查命令 |
| --- | --- | --- |
| Python | 3.11–3.13（统一平台要求 `<3.14`） | `python --version` |
| uv | 最新稳定版 | `uv --version` |
| Node.js | 20 或更高 | `node --version` |
| npm | 随 Node.js 安装 | `npm --version` |
| Docker Desktop | Linux containers 模式 | `docker version` |

首次运行不需要提交或复制虚拟环境：

- 统一后端：`uv sync --all-extras` 自动创建根目录 `.venv`。
- Cairn：在 `Cairn-worker-full\cairn` 项目中执行 `uv sync`，自动创建 Cairn 的 `.venv`。
- 前端：`npm ci` 根据 `frontend/package-lock.json` 生成 `frontend/node_modules`。
- 当前交付分支已包含一份前端依赖快照；依赖不一致时仍应重新执行 `npm ci`。

## 二、端口总表

| 服务 | 本地开发默认 | Docker/外接场景 | 说明 |
| --- | ---: | ---: | --- |
| 统一平台前端 | `127.0.0.1:5173` | `127.0.0.1:5173` | Vite 或 Nginx |
| 统一平台 API | `127.0.0.1:8001` | 宿主机 `127.0.0.1:8000` | REST、WebSocket、任务编排 |
| 逆向外接模块 | `127.0.0.1:8002` | 按实际部署修改 | 健康检查通常为 `/health/live` |
| Cairn 黑板/渗透服务 | `127.0.0.1:8000` | 建议映射为 `127.0.0.1:8011` | Cairn API 的 `/projects` |
| PostgreSQL | 不启用 | Compose 内部 `5432` | 持久化数据库 |
| Qdrant | 不启用 | Compose 内部 `6333` | 可选向量检索 |
| 评测仪表盘（可选） | `127.0.0.1:3001` | 外部配置 | 仅基准评测使用 |

本地模式下统一 API 使用 `8001`，Cairn 可使用 `8000`。Docker 模式下统一 API 映射宿主机
`8000`，不能再让 Cairn 占用同一宿主端口，应把 Cairn 映射为 `8011:8000` 并同步修改环境变量。

## 三、配置文件与默认路径

### 统一平台

- `.env.example`：环境变量模板，复制为根目录 `.env`。
- `docker-compose.yml`：前端、API、PostgreSQL、Qdrant 编排。
- `Dockerfile`：API 镜像，容器内部工作目录为 `/app`。
- `frontend/vite.config.js`：前端固定监听 `5173`，后端代理默认指向 `http://127.0.0.1:8001`。
- `frontend/nginx.conf`：Docker 前端把 `/api` 和 `/health` 代理到 `api:8000`。

本地默认数据路径（相对项目根目录）：

```text
data/inputs
data/uploads
data/runs
data/evaluations
data/question-banks
data/mcp-generated
data/security_agent.db
```

Docker 容器中的对应路径为：

```text
/app/data/inputs
/app/data/uploads
/app/data/runs
/app/data/evaluations
```

### Cairn 外接运行时

外接目录示例：

```text
C:\kaifa\tool\Cairn-worker-full
C:\kaifa\tool\Cairn-worker-full\cairn
```

主要文件：

- `cairn/pyproject.toml`、`cairn/uv.lock`：Cairn Python 依赖。
- `docker-compose.yaml`：`cairn-server` 与 `cairn-dispatcher`。
- `dispatch.local.yaml`：本地 Dispatcher 配置；密钥应通过环境变量注入。
- `dispatch.yaml`：Docker Dispatcher 配置。
- Cairn 默认 SQLite 数据库：`%USERPROFILE%\.local\share\cairn\cairn.db`。
- Docker 数据挂载：`datas/cairn/` → 容器 `/root/.local/share/cairn/`。

### 写死或约定的路径说明

以下是代码或 Docker 中的运行约定，不是必须固定的物理路径：

- 统一平台相对路径以启动目录为基准，建议始终在仓库根目录执行命令。
- Docker API 工作目录固定为 `/app`，前端容器静态目录为 `/usr/share/nginx/html`。
- Cairn Dispatcher 在 Docker 中读取 `/cairn/dispatch.yaml`。
- Cairn Docker Dispatcher 如需访问宿主机 Docker，会挂载 `/var/run/docker.sock`。
- 题库、上传和运行产物均由 `.gitignore` 排除，不应提交到远程仓库。

## 四、统一平台本地首次启动

```powershell
Set-Location C:\kaifa\tool\my-competition-secmind-baseline-f606c48
uv sync --all-extras
Copy-Item .env.example .env
$env:SECURITY_AGENT_API_HOST = "127.0.0.1"
$env:SECURITY_AGENT_API_PORT = "8001"
$env:SECURITY_AGENT_DEMO_MODE = "true"
uv run security-agent-api
```

另开 PowerShell 启动前端：

```powershell
Set-Location C:\kaifa\tool\my-competition-secmind-baseline-f606c48\frontend
npm ci
$env:VITE_DEV_BACKEND_TARGET = "http://127.0.0.1:8001"
npm run dev
```

访问：

- 工作台：<http://127.0.0.1:5173>
- API 文档：<http://127.0.0.1:8001/docs>
- API 健康检查：<http://127.0.0.1:8001/health>

## 五、模型 API 配置

默认 `SECURITY_AGENT_DEMO_MODE=true`，无需 API Key。需要真实模型时，在本机 `.env` 或 PowerShell
环境变量中设置：

```dotenv
SECURITY_AGENT_DEMO_MODE=false
SECURITY_AGENT_MODEL_BASE_URL=https://api.deepseek.com
SECURITY_AGENT_MODEL_API_KEY=只在本机填写真实密钥
SECURITY_AGENT_PLANNER_MODEL=实际可用的模型ID
SECURITY_AGENT_WORKER_MODEL=实际可用的模型ID
SECURITY_AGENT_FALLBACK_MODEL=实际可用的模型ID
```

`MODEL_BASE_URL` 必须是完整的 HTTP/HTTPS 地址，不要写 Markdown 方括号、查询参数或
`/chat/completions`。真实密钥不得写入 Git、Dockerfile 或 `dispatch*.yaml`。

## 六、启动 Cairn 黑板/渗透模块

### Docker 方式

```powershell
Set-Location C:\kaifa\tool\Cairn-worker-full
uv sync --project cairn
docker compose -f docker-compose.yaml up -d
docker compose -f docker-compose.yaml ps
```

默认 Cairn API 为 `http://127.0.0.1:8000`。本地统一 API 使用 `8001` 时，在统一平台 `.env`
中配置：

```dotenv
SECURITY_AGENT_PENETRATION_BASE_URL=http://127.0.0.1:8000
SECURITY_AGENT_REVERSE_BASE_URL=http://127.0.0.1:8002
```

如果统一平台也使用 Docker，请把 Cairn compose 的端口映射改成 `8011:8000`，然后设置：

```dotenv
SECURITY_AGENT_PENETRATION_BASE_URL=http://127.0.0.1:8011
```

### 本地进程方式

```powershell
Set-Location C:\kaifa\tool\Cairn-worker-full
uv run --project cairn cairn serve --host 127.0.0.1 --port 8000
```

另开窗口启动 Dispatcher：

```powershell
Set-Location C:\kaifa\tool\Cairn-worker-full
uv run --project cairn cairn dispatch --config dispatch.local.yaml
```

检查 Cairn：

```powershell
Invoke-WebRequest http://127.0.0.1:8000/projects
```

## 七、启动逆向外接模块

逆向模块默认由独立服务提供，统一平台只通过 HTTP 调用：

```dotenv
SECURITY_AGENT_REVERSE_BASE_URL=http://127.0.0.1:8002
SECURITY_AGENT_MODULE_TIMEOUT_SECONDS=20
SECURITY_AGENT_PENETRATION_POLL_INTERVAL_SECONDS=3
SECURITY_AGENT_PENETRATION_POLL_TIMEOUT_SECONDS=540
```

启动后确认 `http://127.0.0.1:8002/health/live` 可访问。逆向模块未启动时，代码审计、单文件分析和
应急响应仍可使用；渗透任务会根据连接状态返回明确错误。

## 八、Docker Compose 启动统一平台

```powershell
Set-Location C:\kaifa\tool\my-competition-secmind-baseline-f606c48
Copy-Item .env.example .env
# 生产或共享环境必须修改 POSTGRES_PASSWORD
docker compose build
docker compose up -d
docker compose ps
```

访问：<http://127.0.0.1:5173>，API 文档为 <http://127.0.0.1:8000/docs>。

查看日志：

```powershell
docker compose logs -f api
docker compose logs -f frontend
```

停止服务但保留数据：`docker compose down`。`docker compose down -v` 会删除 PostgreSQL、Qdrant
和任务数据卷，执行前必须备份。

## 九、端口冲突排查

```powershell
netstat -ano | Select-String ':5173|:8000|:8001|:8002|:8011'
Get-Process -Id <PID>
Stop-Process -Id <PID>
```

若前端端口需要改变，同时修改 `frontend/package.json` 的 Vite 端口和访问地址；若后端端口改变，
同步设置 `SECURITY_AGENT_API_PORT` 与 `VITE_DEV_BACKEND_TARGET`。

## 十、启动后验收

```powershell
Set-Location C:\kaifa\tool\my-competition-secmind-baseline-f606c48
uv run pytest -q
Set-Location frontend
npm test -- --run
npm run build
```

确认前端能打开工作台、API `/health` 返回 `status=ok`，并在配置了外接服务时分别检查逆向
`/health/live` 和 Cairn `/projects`。
